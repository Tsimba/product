package com.coreserve.product.repository;

import com.coreserve.product.modele.PrixType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrixTypeRepository extends JpaRepository<PrixType, Long> {
}
